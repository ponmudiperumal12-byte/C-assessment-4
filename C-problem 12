#include <stdio.h>
int main()
{
    int a, count = 0;
    printf("enter the number:");
    scanf("%d", &a);
    loop: if(a > 0)
    {
        count++;
        a = a / 10;
        goto loop;
    }
    printf("%d", count);
}
