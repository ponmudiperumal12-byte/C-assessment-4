#include <stdio.h>
int main()
{
    int a = 5, odd, f, s, b;
    loop: if (a < 50)
    {
     odd = (2 * a + 1);
     a++;
     f = odd % 10;     
     s = odd / 10; 
     b=f+s;
     if(b==7)
     {
       printf("%d \n", s * 10 + f);
     }
    goto loop;
   }

}
