#include <stdio.h>
int main()
{
    int a, rev = 0;
    printf("enter the number:");
    scanf("%d", &a);
    loop: if(a > 0)
    {
        rev=rev*10+(a%10);
        a = a / 10;
        goto loop;
    }
    printf("%d", rev);
}
