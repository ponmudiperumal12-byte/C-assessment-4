#include <stdio.h>
int main()
{
    int a , f, s, t, f0;
     printf("enter the number:");
     scanf("%d",&a);
     f=a%10;
     s=(a/10)%10;
     t= (a/ 100)%10;
     f0=(a/100)%10;
     printf("%d \n", f*1000+t*100+s*10+f0);
}
